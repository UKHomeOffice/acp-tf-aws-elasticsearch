#!/bin/bash

%{ for tenant in tenant_list }
curl -XPUT -u "${es_user}:${es_pass}" "https://${aws_es_endpoint}/_opendistro/_security/api/tenants/${tenant}" -H 'Content-Type: application/json' -d '
{
  "description": "A kibana tenant for the ${tenant} team."
}
'
%{ endfor ~}



